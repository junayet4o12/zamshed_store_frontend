import React from 'react';

const ButtonStrongMini = ({text}) => {
    return (
        <p className='px-5 py-2 rounded-full bg-primary/90  hover:bg-primary text-white font-medium transition-all duration-200 cursor-pointer text-center'>{text}</p>
    );
};

export default ButtonStrongMini;